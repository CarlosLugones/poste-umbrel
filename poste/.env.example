# Copy this file to .env and fill in your values.
# The .env file must be placed in the same directory as docker-compose.yml.

# The fully-qualified hostname for your mail server.
# This must match the MX record in your DNS and the hostname used for SSL certs.
# Example: MAIL_HOSTNAME=mail.yourdomain.com
MAIL_HOSTNAME=mail.example.com

# Timezone (used by poste.io for log timestamps and scheduling).
# See https://en.wikipedia.org/wiki/List_of_tz_database_time_zones for valid values.
TZ=UTC
